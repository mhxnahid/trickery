# Intro
```
We install ubuntu 22.04 using an iso image. Burn it using `woeusb` in linux or `rufus` in windows.
Create separate /home (ext4) / (ext4) and /boot (ext2) partitions. Give /boot around 2GB to be safe.
```
# Essentials
```
apt install 
```
